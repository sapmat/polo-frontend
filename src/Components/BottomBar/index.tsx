const BottomBar = () => {
  return (
    <div className="bottom-bar">
      {/* song playing */}
      {/* player */}
      {/* open current song - show queue - volume bar */}
    </div>
  );
};

export default BottomBar;
